{
  "name": "squint-demo",
  "private": true,
  "scripts": {
    "build": "yarn vite build",
    "serve": "yarn vite preview"
  },
  "type": "module",
  "dependencies": { },
  "devDependencies": {
    "vite": "^5.1.0",
    "vite-plugin-squint": "file:.."
  }
}
